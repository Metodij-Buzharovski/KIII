apiVersion: v1
kind: Service
metadata:
    name: "kubecluster-a238"
    labels:
        app: "kubecluster-a238"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "kubecluster-a238"